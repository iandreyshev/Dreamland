<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.iandreyshev.dreamland">

    <application
        android:name=".application.DreamlandApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--ACTIVITY-->
        <activity
            android:name=".presentation.activity.SplashActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ru.iandreyshev.featureAccount.presentation.activity.AuthActivity"
            android:noHistory="true"
            android:theme="@style/AppTheme" />
        <activity android:name="ru.iandreyshev.featureMenu.presentation.activity.MenuActivity" />
        <!--ACTIVITY-->
    </application>

</manifest>