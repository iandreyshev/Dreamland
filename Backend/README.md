# Server
Проект сервера
## Сборка
Запустить файл BuildServer.cmd с версией сборки
`BuildServer.cmd 1.0.0-SNAPSHOT` 

В случае успеха создастся папка Build, внутри которой будет папка с сборкой `Server_v*/` два скрипта:
* run.cmd - запускает сервер
* stop.cmd - останавливает работу сервер